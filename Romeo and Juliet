#include <stdio.h>

int main(){
    int X,Y,Z;
    scanf("%d %d %d",&X,&Y,&Z);
    int total_money = (X * 5) + (Y *10);
    int max_chocolates = total_money/Z;
    printf("%d",max_chocolates);
    return 0;
}

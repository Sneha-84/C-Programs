#include <stdio.h>
int main(){
    int CP,SP;
    scanf("%d %d",&CP, &SP);
    double Loss = (double)CP - SP;
    double LossPercentage = (Loss /CP)*100;
    printf("%.2f", LossPercentage);
    return 0;
}

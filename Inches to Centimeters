#include <stdio.h>
#define CM_PER_INCH 2.54
int main() {
    int inches;
    double centimeters;
    scanf("%d", &inches);
    centimeters = inches * CM_PER_INCH;
    printf("%.2f", centimeters);
    return 0;
}

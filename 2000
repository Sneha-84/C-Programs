#include <stdio.h>
int main() {
    int N;
    int total_amount;
    int notes_500;
    scanf("%d", &N);
    total_amount = N * 2000;
    notes_500 = total_amount / 500;
    printf("%d
", notes_500);
    return 0;
}

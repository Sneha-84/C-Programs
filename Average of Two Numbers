#include <stdio.h>
int main(){
    float num1, num2, average;
    scanf("%f %f", &num1, &num2);
    average = (num1 + num2)/2.0;
    printf("Average of %.0f and %.0f is: %.2f", num1, num2,
    average);
    return 0;
}
